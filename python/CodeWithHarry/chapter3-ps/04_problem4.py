# 4.replace the double space from problem 3 with a single space
name = "ankit is a good  boy and "
print(name.replace("  ", " "))
print(name) # string are immutable which means that you cannot change them by running function on them